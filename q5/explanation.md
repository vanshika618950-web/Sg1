# Question 5 Explanation

This task assessed the available storage devices, mounted file systems, disk usage, and inode usage. The command outputs showed several block devices, the mounted overlay and temporary filesystems, and the current disk and inode utilization across the environment.

The results indicate that storage capacity is available overall, but some filesystems are more heavily used than others. These observations support recommendations such as cleaning old files, monitoring growth, and using additional storage if the workload increases.
